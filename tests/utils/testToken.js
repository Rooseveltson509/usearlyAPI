/* import jwt from "jsonwebtoken";

const JWT_SIGN_SECRET = "test-secret"; // Utilisez une clé secrète de test

export function generateTestToken(userId) {
  return jwt.sign({ userId }, JWT_SIGN_SECRET, { expiresIn: "1h" });
} */
